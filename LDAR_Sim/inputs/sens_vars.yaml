sens_z_var:
  # paths:
  #   - 'P_air.methods.aircraft.cost.per_day'
  #   - 'P_air_dev.methods.aircraft_dev.cost.per_day'
  # vals: [1000, 10000]
      
sens_x_vars:
  - paths:
      - '__all.LPR'
    vals: [0.00065, 0.0065]
  - paths:
      - '__all.NRd'
    vals: [50, 150, 365]
  - paths:
      - '__all.leak_file'
    vals: ["", 'leak_rates.csv']

stat_columns:
  - column: 'emis_rat'
    description: 'Daily total cost difference($)'
  - column: 'cost_diff'
    description: 'Daily emissions ratio'
  - column: 'daily_emissions_kg'
    description: 'Daily emissions (kg/day)'
  - column: 'total_daily_cost'
    description: 'Daily total cost ($)'

